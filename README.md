# Flask To-Do App with MongoDB

A simple full-stack To-Do application built using Flask as the backend, MongoDB Atlas for the database, and HTML/CSS/JavaScript on the frontend. Tasks are stored with a selected date and can be added, viewed, updated, or deleted.



